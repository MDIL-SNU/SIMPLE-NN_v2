==========
calc_scale
==========

- ``True`` (default) / ``False``
----

**calc_scale** determines whether users are going to calculate scaling parameters or not. Scaling parameter is important ~~ 설명 필요
