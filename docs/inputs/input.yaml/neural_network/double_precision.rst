================
double_precision
================

- ``True`` (default) / ``False``

----

**double_precision** determines whether the double-precision(``True``) or single-precision(``False``) is used.
