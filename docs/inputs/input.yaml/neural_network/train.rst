=====
train
=====

- ``True`` (default) / ``False``
----

``train`` determines whether SIMPLE-NN conducts training or not. For the test process or drawing correlation graph ``train`` tag must be set as ``False``.
